@model IEnumerable<StudioVeterinario.Models.Utente>

@{
    ViewBag.Title = "Index";
}

<h2>Lista Dipendenti</h2>

<p>
    @Html.ActionLink("Registra Dipendente", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pwd)
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Username)
            </td>
            <td>
                @Html.HiddenFor(modelItem => item.Pwd)
                ********
            </td>

            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID_utente  }) |
                @Html.ActionLink("Details", "Details", new {  id=item.ID_utente }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID_utente })
            </td>
        </tr>
    }

</table>
